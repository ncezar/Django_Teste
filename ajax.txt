 <script>
        $("#id_item").change(function () {
          console.log('Mãe to na funcao', item
          var url = $("#CadastroForm").attr("data-url");
          var item = $(this).val();
          console.log('Mãe to na funcao again ', item)

          $.ajax({                       // initialize an AJAX request
                url: url,                    // set the url of the request (= localhost:8000/hr/ajax/load-cities/)
                data: {
                  'item': item       // add the country id to the GET parameters
                },
                //console.log('Mãe to na funcao dos dados ',data)
                success: function (data) {   // `data` is the return of the `load_cities` view function
                  $("#preco_unitario").html(data);  // replace the contents of the city input with the data that came from the server
                  //$('id_item').change

                  //$('id_preco_unitario').val(data[preco_unitario])
                  //val(data['preco_unitario'])
                  // envia valor da view data-url pra dentro do input do html (inspecionar elemento item pois ele que dispara o evento)
                }
          });
